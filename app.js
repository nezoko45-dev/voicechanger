(()=>{const $=id=>document.getElementById(id),status=$("status"),transcript=$("transcript");let ctx,stream,source,processor,stt,tts,running=false,loaded=false,audioQueue=Promise.resolve();function say(t,k=""){status.textContent=t;status.className="status "+k}function authSocket(url){const token=$("key").value.trim();if(!token)throw Error("Enter a Deepgram API key or temporary token.");return new WebSocket(url,["token",token])}function resample(a,from,to){if(from===to)return a;const r=from/to,o=new Float32Array(Math.max(1,Math.round(a.length/r)));for(let i=0;i<o.length;i++){const p=i*r,x=Math.floor(p),y=Math.min(x+1,a.length-1),f=p-x;o[i]=(a[x]||0)*(1-f)+(a[y]||0)*f}return o}function pcm(a){const o=new Int16Array(a.length);for(let i=0;i<a.length;i++)o[i]=Math.max(-32768,Math.min(32767,Math.round(a[i]*32767)));return o}async function loadVoice(){const ref=$("ref").files[0];if(!ref)return say("Select your reference WAV first.","err");try{$("load").disabled=true;say("Loading reference into local OpenVoice V2...");const b=new FormData;b.append("reference",ref,ref.name);const r=await fetch("/api/reference",{method:"POST",body:b});if(!r.ok)throw Error(await r.text());loaded=true;$("start").disabled=false;say("Voice loaded. Press Start listening.","ok")}catch(e){loaded=false;$("start").disabled=true;say("Voice load error: "+e.message,"err")}finally{$("load").disabled=false}}function connectSTT(){const q=new URLSearchParams({model:"nova-3",language:"en-US",encoding:"linear16",sample_rate:"16000",channels:"1",interim_results:"true",smart_format:"true",endpointing:"500"});stt=authSocket("wss://api.deepgram.com/v1/listen?"+q);stt.binaryType="arraybuffer";stt.onopen=()=>say("Listening continuously...","ok");stt.onerror=()=>say("Deepgram STT WebSocket error.","err");stt.onclose=()=>running&&say("Deepgram STT disconnected.","err");stt.onmessage=e=>{if(typeof e.data!=="string")return;try{const m=JSON.parse(e.data),t=m.channel?.alternatives?.[0]?.transcript||"";if(t)transcript.textContent=t;if(m.type==="Results"&&m.is_final&&m.speech_final&&t.trim())audioQueue=audioQueue.then(()=>speakAndClone(t.trim())).catch(x=>say("Pipeline error: "+x.message,"err"))}catch{}}}function connectTTS(){const model=$("voice").value.trim()||"aura-2-asteria-en",q=new URLSearchParams({model,encoding:"linear16",sample_rate:"22050"});tts=authSocket("wss://api.deepgram.com/v1/speak?"+q);tts.binaryType="arraybuffer";tts.onerror=()=>say("Deepgram TTS WebSocket error.","err")}async function speakAndClone(text){if(!tts||tts.readyState!==WebSocket.OPEN)throw Error("TTS WebSocket is not connected.");say("Deepgram TTS → OpenVoice: "+text);const chunks=[];await new Promise((resolve,reject)=>{const old=tts.onmessage,timer=setTimeout(()=>{tts.onmessage=old;reject(Error("TTS timed out."))},30000);tts.onmessage=e=>{if(typeof e.data!=="string"){chunks.push(e.data);return}try{const m=JSON.parse(e.data);if(m.type==="Flushed"){clearTimeout(timer);tts.onmessage=old;resolve()}if(m.type==="Error"){clearTimeout(timer);tts.onmessage=old;reject(Error(m.description||"Deepgram TTS error"))}}catch{}};tts.send(JSON.stringify({type:"Speak",text}));tts.send(JSON.stringify({type:"Flush"}))});const n=chunks.reduce((a,b)=>a+b.byteLength,0),pcmBytes=new Uint8Array(n);let p=0;for(const c of chunks){pcmBytes.set(new Uint8Array(c),p);p+=c.byteLength}const r=await fetch("/api/convert",{method:"POST",headers:{"Content-Type":"application/octet-stream"},body:pcmBytes});if(!r.ok)throw Error(await r.text());await play(await(await r.blob()).arrayBuffer());say("Playing cloned audio.","ok")}async function play(buf){if(!ctx)ctx=new AudioContext;if(ctx.state==="suspended")await ctx.resume();const b=await ctx.decodeAudioData(buf.slice(0)),s=ctx.createBufferSource();s.buffer=b;s.connect(ctx.destination);s.start();await new Promise(r=>s.onended=r)}async function start(){if(!loaded)return say("Load the reference voice first.","err");try{$("start").disabled=true;$("stop").disabled=false;running=true;ctx=new AudioContext;await ctx.resume();stream=await navigator.mediaDevices.getUserMedia({audio:{channelCount:1,echoCancellation:false,noiseSuppression:false,autoGainControl:false}});source=ctx.createMediaStreamSource(stream);processor=ctx.createScriptProcessor(2048,1,1);processor.onaudioprocess=e=>{if(!stt||stt.readyState!==WebSocket.OPEN)return;const a=resample(e.inputBuffer.getChannelData(0),ctx.sampleRate,16000);stt.send(pcm(a).buffer)};source.connect(processor);processor.connect(ctx.destination);connectSTT();connectTTS()}catch(e){stop();say("Start error: "+e.message,"err")}}function stop(){running=false;try{processor?.disconnect()}catch{}try{source?.disconnect()}catch{}try{stream?.getTracks().forEach(t=>t.stop())}catch{}try{stt?.close()}catch{}try{tts?.close()}catch{}processor=source=stream=stt=tts=null;$("stop").disabled=true;$("start").disabled=!loaded;say(loaded?"Stopped. Voice remains loaded.":"Select a reference WAV.")}$("load").onclick=loadVoice;$("start").onclick=start;$("stop").onclick=stop;addEventListener("beforeunload",stop)})();